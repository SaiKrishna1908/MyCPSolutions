#include <iostream>
#include <bits/stdc++.h>

#define ll long long

int main() {

  ios::sync_with_stdin(0);
  cin.tie(0);
  cout.tie(0);
  cout<<fixes;

  int n,a,x;
  cin >> n >> a >> x;

  // b is count of ones 
  int b = n-a; 

  int divn += x
}
